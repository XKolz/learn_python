# List Methods
numbers = [1,2,3,4,5]
# numbers.append(6), adds the 
# numbers.insert(0,-1), this edits the index of the specific object
# numbers.remove(3), deletes the index  of that particular object
# numbers.clear(), clears the emtire array, doesn't expect an value
# print(1 in numbers), prints a boolean (checks if the expression is present)
# print(len(numbers)), prints return the number if objects in the array
# print(numbers)